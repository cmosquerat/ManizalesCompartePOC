name: manizales_comparte_app
description: Manizales Comparte App - POC
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  provider:
  flutter_map:
  latlong2:
  google_fonts:
  flutter_svg:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/fotos/
